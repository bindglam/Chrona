--- a/net/minecraft/core/AxisCycle.java
+++ b/net/minecraft/core/AxisCycle.java
@@ -35,7 +_,17 @@
 
         @Override
         public Direction.Axis cycle(Direction.Axis axis) {
-            return AXIS_VALUES[Math.floorMod(axis.ordinal() + 1, 3)];
+            // Chrona start - Lithium - fast util
+            return switch (axis.ordinal()) {
+                case 0 -> // X
+                        Direction.Axis.Y;
+                case 1 -> // Y
+                        Direction.Axis.Z;
+                case 2 -> // Z
+                        Direction.Axis.X;
+                default -> throw new IllegalArgumentException();
+            };
+            // Chrona end - Lithium - fast util
         }
 
         @Override
@@ -56,7 +_,17 @@
 
         @Override
         public Direction.Axis cycle(Direction.Axis axis) {
-            return AXIS_VALUES[Math.floorMod(axis.ordinal() - 1, 3)];
+            // Chrona start - Lithium - fast util
+            return switch (axis.ordinal()) {
+                case 0 -> //X
+                        Direction.Axis.Z;
+                case 1 -> //Y
+                        Direction.Axis.X;
+                case 2 -> //Z
+                        Direction.Axis.Y;
+                default -> throw new IllegalArgumentException();
+            };
+            // Chrona end - Lithium - fast util
         }
 
         @Override
@@ -64,6 +_,15 @@
             return FORWARD;
         }
     };
+
+    // Chrona start - Lithium - fast util
+    static {
+        assert Direction.Axis.X.ordinal() == 0;
+        assert Direction.Axis.Y.ordinal() == 1;
+        assert Direction.Axis.Z.ordinal() == 2;
+        assert Direction.Axis.values().length == 3;
+    }
+    // Chrona end - Lithium - fast util
 
     public static final Direction.Axis[] AXIS_VALUES = Direction.Axis.values();
     public static final AxisCycle[] VALUES = values();
